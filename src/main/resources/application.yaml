spring:
  datasource:
    username: cat_adoption_clinic
    password: cat_adoption_clinic
    url: jdbc:mysql://localhost:3306/cat_adoption_clinic
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    defer-datasource-initialization: true

  sql:
    init:
      mode: always